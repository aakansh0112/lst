<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Log In</title>
    <style>
        /* Reset and Base Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        /* Body Styling */
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(to right,rgb(203, 162, 141),rgb(157, 130, 106));
        }

        /* Login Container */
        .login-container {
            background: #f4e1d2;
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.3);
            text-align: center;
            width: 100%;
            max-width: 380px;
            animation: fadeIn 0.8s ease-in-out;
        }

        /* Fade In Animation */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* Heading */
        h2 {
            margin-bottom: 1rem;
            color: #5a4033;
            font-size: 1.8rem;
        }

        /* Input Fields */
        input {
            width: 100%;
            padding: 12px;
            margin: 10px 0;
            border: 1px solid #8b6f56;
            border-radius: 6px;
            font-size: 1rem;
            background: #fdf6f0;
        }

        input:focus {
            outline: none;
            border-color: #6b4f3d;
            box-shadow: 0 0 5px rgba(138, 110, 87, 0.5);
        }

        /* Button */
        button {
            width: 100%;
            padding: 12px;
            background:rgb(147, 109, 86);
            border: none;
            color: white;
            font-size: 1rem;
            cursor: pointer;
            border-radius: 6px;
            transition: 0.3s;
        }

        button:hover {
            background:rgb(135, 100, 82);
        }

        /* Signup Link */
        p {
            margin-top: 1rem;
            font-size: 0.9rem;
            color:rgb(171, 143, 127);
        }

        a {
            color: #7b5d4a;
            text-decoration: none;
            font-weight: bold;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>

    <div class="login-container">
        <h2>Log In</h2>
        <form action="/login" method="post">
            <input type="text" name="name" placeholder="Name" required>
            <input type="password" name="password" placeholder="Password" required>
            <button type="submit">Log In</button>
        </form>
        <p>Don't have an account? <a href="/signup">Sign Up</a></p>
    </div>

</body>
</html>
